---
title: Quick start
description: One command sets your app up for conciv. Or pick your bundler and wire it by hand.
icon: Zap
---

## One command

```bash
npx conciv@latest init
```

init detects your bundler, package manager, and installed agent harnesses, then prints the whole
plan and asks once before it writes anything. On approval it installs `@conciv/it`, wires your
config, and teaches your agents the conciv CLI, resolving each step in place as it goes and printing
a real diff of every config it edited. Anything it cannot do for you comes back as a card with the
snippet to paste.

`--yes` approves the plan up front, which is what you want in CI or an agent session: without a real
terminal init stops and says so instead of waiting on an invisible prompt. `--dry-run` prints the
plan and exits without touching anything. init refuses a dirty git tree; commit first or pass
`--force`.

## Prerequisites

<Callout>
  You need a dev build of your app, Node 22 or newer, and the `claude` CLI on your `PATH`. The chat uses `claude` to
  answer. See [Harnesses](/docs/harnesses) to use a different agent.
</Callout>

conciv mounts in dev only. The conciv button shows once the engine routes are live, so it stays inert on
a plain production preview.

## Pick your framework

<Cards>
  <Card
    title="Vite"
    href="/docs/quick-start/vite"
    icon={<img src="/icons/vite.svg" alt="" className="size-4" />}
    description="Full integration. Live page bridge and widget injection."
  />
  <Card
    title="Next.js"
    href="/docs/quick-start/nextjs"
    icon={<img src="/icons/nextjs.svg" alt="" className="size-4" />}
    description="withConciv plus the client widget."
  />
  <Card
    title="webpack"
    href="/docs/quick-start/webpack"
    icon={<img src="/icons/webpack.svg" alt="" className="size-4" />}
    description="Bridge-less engine. Inject the widget yourself."
  />
  <Card
    title="Rspack"
    href="/docs/quick-start/rspack"
    icon={<img src="/icons/rspack.svg" alt="" className="size-4" />}
    description="Bridge-less engine. Inject the widget yourself."
  />
  <Card
    title="iOS (alpha)"
    href="/docs/quick-start/ios"
    icon={<img src="/icons/apple.svg" alt="" className="size-4" />}
    description="Native app in the simulator. Swift SDK loads the widget from the dev core."
  />
</Cards>

Using Rollup or esbuild? See the [Rollup](/docs/quick-start/rollup) and
[esbuild](/docs/quick-start/esbuild) notes.

Working with coding agents? [Connect your agents](/docs/quick-start/agents) covers what init
teaches them and how to wire it by hand.
