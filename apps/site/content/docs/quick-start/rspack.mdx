---
title: Rspack
description: Add mandarax to an Rspack app.
---

Rspack works like webpack. The entry boots the engine, and you inject the widget yourself with
`widgetUrl`.

<Steps>
<Step>

### Install

```bash
npm install -D mandarax
```

</Step>
<Step>

### Add the plugin

```js title="rspack.config.js"
const mandarax = require('mandarax/plugin/rspack')

module.exports = {
  plugins: [mandarax.default()],
}
```

</Step>
<Step>

### Inject the widget

The widget bundle ships as `@mandarax/widget/global`. Serve it and set `widgetUrl` so the script is
added to your page. See [Configuration](/docs/configuration#widgeturl).

</Step>
</Steps>

<Callout>
  Rspack is less complete than [Vite](/docs/quick-start/vite). The live page bridge is Vite only. Chat, tests, and
  approvals work once the engine is booted and the widget is on the page.
</Callout>
