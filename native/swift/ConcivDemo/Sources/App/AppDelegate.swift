import UIKit

// Minimal application delegate. The scene manifest in Info.plist wires the
// UIWindowScene to SceneDelegate, which builds the UI and attaches the widget.
@objc(AppDelegate)
final class AppDelegate: UIResponder, UIApplicationDelegate {}
