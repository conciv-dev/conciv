# @conciv/claude-connect

Generates the conciv claude plugin files (marketplace manifest, plugin manifest, MCP bridge) and
reads whether claude has them installed for a project. Used by the `conciv` CLI and
`@conciv/harness`.

Part of [conciv](https://github.com/conciv-dev/conciv).
