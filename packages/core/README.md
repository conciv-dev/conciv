# @mandarax/core

The mandarax engine: a framework-free h3 + srvx server exposing the /\_\_pw HTTP surface, with harness + test-runner registry seams.

Part of [mandarax](https://github.com/mandarax-dev/mandarax).
