# @mandarax/tool-ui

SolidJS tool-call renderers for the mandarax chat thread. tanstack-AI convention: render by tool name, reading zod-typed part.input/part.output. One view per tool, viewable in Storybook.

Part of [mandarax](https://github.com/mandarax-dev/mandarax).
