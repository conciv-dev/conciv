---
'@conciv/ui-kit-chat': patch
---

Narrate what the agent is doing instead of going silent: a pinned now-line above the composer names the running tool, thinking-only segments keep a readable trace, and the collapsed trace line reports the current activity while a run streams. The session status bar gains a pulsing running dot and a clearly marked active view tab, and the panel header switches to padding-driven spacing.
