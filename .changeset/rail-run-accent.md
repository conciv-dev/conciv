---
'@conciv/ui-kit-chat': patch
---

Trace rail: the accent now marks the running step and nothing else. Each row owns its inbound connector, liveness is derived from tool status instead of "last part while the turn streams", the scroll reading-band highlight that shared the accent's styling is gone, a collapsed trace draws no rail at all, and a resolved permission request no longer leaves an empty row behind that shifted the rail's arms.
