---
'@conciv/embed': patch
---

The grab-selector reference card now clears the moment a message is sent instead of lingering until the AI answers, a refused send restores the staged grabs instead of losing them, and the element preview is height-bounded (160px default) with a keyboard-operable resize handle.
