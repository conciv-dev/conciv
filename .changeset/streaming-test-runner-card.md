---
'@conciv/extension-test-runner': patch
---

test_runner card streams the run: per-test rows and pass/fail counts now update live from the runner's event stream instead of appearing only when the run finishes.
