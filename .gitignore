node_modules/
dist/
build/
*.tsbuildinfo
.turbo/
coverage/
.DS_Store
*.log
.idea/
.superpowers/
.design-ref/
.playwright-cli/
# conciv runtime state the vite plugin writes into a consuming app's root
.conciv/
# stale runtime state from the old `aidx` tool name (pre-rename); never commit
.aidx/
# Claude Code local worktrees (git worktrees created under the repo)
.claude/worktrees/

*storybook.log
storybook-static

# vitest browser failure screenshots (regenerated on failure, never committed)
**/test/__screenshots__/
**/.vitest-attachments/

# playwright run output (regenerated per run, never committed)
**/test-results/

# per-turn MCP bridge configs the tanstack claude adapter writes into cwd (cleaned up after each turn)
.tanstack-mcp-bridge-*.json

.migration-stash/
